{
	"name": "code-atlas",
	"displayName": "code-atlas",
	"description": "",
	"version": "0.0.1",
	"engines": {
		"vscode": "^1.105.0"
	},
	"categories": [
		"Other"
	],
	"activationEvents": [
		"onLanguage:markdown"
	],
	"main": "./dist/extension.js",
	"contributes": {
		"commands": [
			{
				"command": "codeAtlas.pinSelection",
				"title": "Code Atlas: Pin Selection"
			},
			{
				"command": "codeAtlas.linkPin",
				"title": "Code Atlas: Link Pin"
			}
		],
		"keybindings": [
			{
				"command": "codeAtlas.pinSelection",
				"key": "ctrl+shift+p",
				"when": "editorTextFocus"
			},
			{
				"command": "codeAtlas.linkPin",
				"key": "ctrl+shift+l",
				"when": "editorTextFocus"
			}
		]
	},
	"scripts": {
		"vscode:prepublish": "npm run package",
		"compile": "npm run check-types && npm run lint && node esbuild.js",
		"watch": "npm-run-all -p watch:*",
		"watch:esbuild": "node esbuild.js --watch",
		"watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
		"package": "npm run check-types && npm run lint && node esbuild.js --production",
		"compile-tests": "tsc -p . --outDir out",
		"watch-tests": "tsc -p . -w --outDir out",
		"pretest": "npm run compile-tests && npm run compile && npm run lint",
		"check-types": "tsc --noEmit",
		"fmt": "prettier --write .",
		"fmt:check": "prettier --check .",
		"lint": "eslint src",
		"test": "vscode-test --coverage --coverage-reporter html",
		"install-local": "npx vsce package && code --install-extension code-atlas-0.0.1.vsix"
	},
	"devDependencies": {
		"@types/mocha": "^10.0.10",
		"@types/node": "22.x",
		"@types/vscode": "^1.105.0",
		"@vscode/test-cli": "^0.0.12",
		"@vscode/test-electron": "^2.5.2",
		"esbuild": "^0.27.3",
		"eslint": "^9.39.3",
		"npm-run-all": "^4.1.5",
		"prettier": "^3.8.1",
		"typescript": "^5.9.3",
		"typescript-eslint": "^8.56.1"
	}
}
